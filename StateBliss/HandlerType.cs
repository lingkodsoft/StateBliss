namespace StateBliss
{
    public enum HandlerType
    {
        OnEntering,
        OnEntered,
        OnExiting,
        OnExited,
        OnChanging,
        OnChanged,
        OnEditing,
        OnEdited
    }
}