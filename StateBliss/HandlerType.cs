namespace StateBliss
{
    internal enum HandlerType
    {
        OnEntering,
        OnEntered,
        OnExiting,
        OnExited,
        OnChanging,
        OnChanged,
        OnEditing,
        OnEdited
    }
}